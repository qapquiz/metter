# Implementation Plans

Generated by the improve skill on 2026-06-17. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Meteora DLMM API wrapper — `GET /portfolio/open` (Zod-validated) | P1 | M | — | DONE (reviewed/approved) |
| 002  | `getPositionPnl` — `GET /positions/{pool_address}/pnl` (Zod-validated) | P1 | S–M | 001 | TODO |

## Dependency notes

- 001 has no dependencies. It converts the scaffold into a single-endpoint
  wrapper library and is the prerequisite for any future-endpoint plans
  (the `src/client.ts` `request<T>` helper is the extension point later plans
  will build on).
- 002 depends on 001 (DONE): it reuses 001's `MeteoraDlmmClient`, the
  `request<T>(url, schema)` / `buildUrl(path, query)` helpers, and the
  `z.ZodType<T>` schema conventions unchanged — purely additive (new types,
  one new method, new tests). No edits to 001's logic.

## Findings considered and rejected

- **Wrapping all 19 endpoints now** — rejected: the user explicitly scoped this
  run to `portfolio/open` only. Future endpoints become one method + one types
  block on top of this plan's structure.
- **Adding a runtime HTTP/throttle/retry/caching dependency** — rejected: the
  wrapper uses the global `fetch` (Node 18+/Bun/Deno/browsers) with no HTTP
  client lib, and does no throttling/retrying/caching/auth. `zod` is the SOLE
  runtime dependency, added on explicit user request for runtime response
  validation. No other deps.
- **Automated pagination iterator / async generator** — rejected for now: the
  response already exposes `page`/`pageSize`/`hasNext`/`totalCount`, so callers
  can paginate manually. Deferred to a follow-up plan if/when desired.
- **The pre-existing CI/release findings from the prior audit pass** (npm publish
  missing `NODE_AUTH_TOKEN`, unpinned `bun-version: latest`, the optional
  `typescript` peer dep) — out of scope for this direction task, but remain
  valid findings worth their own plans on request.
